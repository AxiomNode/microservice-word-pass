name: Security

on:
  push:
    branches:
      - main
      - develop
  pull_request:
  schedule:
    - cron: "27 5 * * 1"
  workflow_dispatch:

# Workflow-level permissions stay read-only because the security jobs only need
# repository contents; elevated scopes are granted locally to CodeQL below.
permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  codeql:
    name: CodeQL (javascript-typescript)
    runs-on: ubuntu-latest
    # CodeQL needs to upload SARIF results (`security-events: write`) and read
    # workflow metadata (`actions: read`), but no broader repository writes.
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - uses: actions/checkout@v4
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:javascript-typescript"

  trivy-fs:
    name: Trivy filesystem scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: fs
          scan-ref: .
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: "1"
          skip-dirs: "node_modules,src/node_modules,dist,src/dist,coverage,src/coverage"
